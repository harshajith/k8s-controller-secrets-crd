apiVersion: gitsecret.com/v1
kind: GitSecret
metadata:
  name: git-secret6
  namespace: kube-system
spec:
  appname: git-creds
  organization: 
  label: master
  data: 
    username: "{{fsp.git.username}}"
    password: "{{fsp.git.password}}"